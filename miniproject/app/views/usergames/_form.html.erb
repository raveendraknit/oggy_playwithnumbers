<%= form_for(@usergame) do |f| %>
  <% if @usergame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usergame.errors.count, "error") %> prohibited this usergame from being saved:</h2>

      <ul>
      <% @usergame.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
